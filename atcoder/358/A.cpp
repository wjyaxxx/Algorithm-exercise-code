#include <bits/stdc++.h>
using namespace std;
void cinquick()
{
       ios::sync_with_stdio(0);
       cin.tie(0);
       cout.tie(0);
}
string a,b;
int main()
{
        cinquick();
        cin>>a>>b;
        if(a=="AtCoder"&&b=="Land")
            cout<<"Yes"<<"\n";
        else cout<<"No"<<"\n";
        return 0;
}